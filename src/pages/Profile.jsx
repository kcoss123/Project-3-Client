import React from "react";

const Profile = (props) => {
    return (
        <div className="profile">
            {/* Content of the profile page */}
        </div>
    );
};

export default Profile;
